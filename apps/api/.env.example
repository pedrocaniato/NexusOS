# Database connection (SQLite default, use Postgres/MySQL in production)
DATABASE_URL="file:./dev.db"

# Auth Security
JWT_SECRET="nexus_secret_key_change_this_in_production"

# Server configuration
PORT=3001
